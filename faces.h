typedef enum {
    HERO_FACE = 0,
     JOE_FACE,
  PUSHER_FACE,
   MOMMA_FACE,
     EGG_FACE,
   BLOCK_FACE,
    WALL_FACE,
    HOLE_FACE,
    EMPTY_FACE
} FacedThings;

#define NUM_FACES 9

/* What the faces look like are stored in the
   interface-specific includes (e.g. console.h) */
